{% extends "base.html" %}

{% block title %}과목별 통계{% endblock %}

{% block content %}
<div class="container-fluid">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h1 class="h3">
                <i class="bi bi-bar-chart me-2"></i>과목별 통계
            </h1>
            <p class="text-muted">학년별 진도 현황과 과목별 비교 분석</p>
        </div>
    </div>

    {% if grade_progress %}
    {% for grade, data in grade_progress.items() %}
    <div class="card mb-4">
        <div class="card-header">
            <h5 class="card-title mb-0">
                <i class="bi bi-person-badge me-2"></i>{{ grade }}학년
                <span class="badge bg-primary ms-2">{{ data.total_children }}명</span>
            </h5>
        </div>
        <div class="card-body">
            <div class="row">
                <!-- 국어 진도 현황 -->
                <div class="col-md-6 mb-3">
                    <h6 class="fw-bold text-primary">
                        <i class="bi bi-book me-2"></i>국어 진도
                    </h6>
                    {% if data.korean_pages %}
                    <div class="list-group">
                        {% for page, children in data.korean_pages.items() %}
                        <div class="list-group-item d-flex justify-content-between align-items-center">
                            <div>
                                <strong>{{ page }}페이지</strong>
                                <small class="text-muted ms-2">({{ children|length }}명)</small>
                            </div>
                            <div>
                                {% if children|length >= 2 %}
                                <a href="{{ url_for('page_statistics', grade=grade, subject='korean', page=page) }}" 
                                   class="btn btn-sm btn-outline-primary">
                                    <i class="bi bi-graph-up me-1"></i>비교보기
                                </a>
                                {% else %}
                                <span class="badge bg-secondary">대기중</span>
                                {% endif %}
                            </div>
                        </div>
                        {% endfor %}
                    </div>
                    {% else %}
                    <div class="text-muted">
                        <i class="bi bi-info-circle me-2"></i>기록된 진도가 없습니다.
                    </div>
                    {% endif %}
                </div>

                <!-- 수학 진도 현황 -->
                <div class="col-md-6 mb-3">
                    <h6 class="fw-bold text-success">
                        <i class="bi bi-calculator me-2"></i>수학 진도
                    </h6>
                    {% if data.math_pages %}
                    <div class="list-group">
                        {% for page, children in data.math_pages.items() %}
                        <div class="list-group-item d-flex justify-content-between align-items-center">
                            <div>
                                <strong>{{ page }}페이지</strong>
                                <small class="text-muted ms-2">({{ children|length }}명)</small>
                            </div>
                            <div>
                                {% if children|length >= 2 %}
                                <a href="{{ url_for('page_statistics', grade=grade, subject='math', page=page) }}" 
                                   class="btn btn-sm btn-outline-success">
                                    <i class="bi bi-graph-up me-1"></i>비교보기
                                </a>
                                {% else %}
                                <span class="badge bg-secondary">대기중</span>
                                {% endif %}
                            </div>
                        </div>
                        {% endfor %}
                    </div>
                    {% else %}
                    <div class="text-muted">
                        <i class="bi bi-info-circle me-2"></i>기록된 진도가 없습니다.
                    </div>
                    {% endif %}
                </div>
            </div>
        </div>
    </div>
    {% endfor %}
    {% else %}
    <div class="card">
        <div class="card-body text-center py-5">
            <i class="bi bi-graph-up text-muted" style="font-size: 3rem;"></i>
            <h5 class="text-muted mt-3">통계 데이터가 없습니다</h5>
            <p class="text-muted">아이들의 학습 기록을 입력하면 통계를 확인할 수 있습니다.</p>
            <a href="{{ url_for('add_score') }}" class="btn btn-primary">
                <i class="bi bi-plus-circle me-2"></i>점수 입력하기
            </a>
        </div>
    </div>
    {% endif %}
</div>
{% endblock %} 